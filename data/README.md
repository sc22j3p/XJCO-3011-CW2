# Data Directory

This directory is reserved for generated index files.

The coursework requires submitting the compiled index file created by the `build` command. Keep generated files here, for example:

```text
data/index.json
```

Generated index files are ignored by Git by default, so remember to attach or share the final compiled index file separately when submitting through Minerva.
